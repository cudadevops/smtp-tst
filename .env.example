# Copia este archivo a `.env` y reemplaza los valores de ejemplo por tus credenciales reales.
# Nunca subas el archivo `.env` con credenciales reales al repositorio.

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=tu-usuario
SMTP_PASS=tu-contraseña
SMTP_FROM="Nombre del Remitente <remitente@example.com>"
SMTP_TO=destinatario@example.com

# Opcional: configura el comportamiento de la API.
# BREVO_API_KEY=tu-clave-de-brevo
# API_PORT=7522
# API_HOST=127.0.0.1
# EXTRA_FIELD_LIMIT=30
# SUBJECT_PREFIX="Nuevo mensaje de formulario"
# CORS_ALLOW_ORIGIN=*
# MESSAGE_SUCCESS_BREVO="Correo enviado mediante Brevo."
# MESSAGE_SUCCESS_SMTP="Correo enviado mediante SMTP."
# MESSAGE_ERROR_MISSING_REQUIRED="Faltan campos obligatorios: %fields%"
# MESSAGE_ERROR_INVALID_TYPE="Todos los campos deben ser cadenas de texto."
# MESSAGE_ERROR_EXTRA_LIMIT="Solo se permiten %limit% campos adicionales."
# MESSAGE_ERROR_SEND="No se pudo enviar el correo electrónico."
# MESSAGE_ERROR_NOT_FOUND="Ruta no encontrada."
